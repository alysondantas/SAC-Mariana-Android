<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.alysondantas.sac_mariana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CadastrarActivity" />
        <activity android:name=".EditarActivity" />
        <activity android:name=".CadastrarDoadorActivity" />
        <activity android:name=".EditarDoadorActivity" />
        <activity android:name=".CadastrarDoacaoActivity" />
        <activity android:name=".EditarDoacaoActivity" />
        <activity android:name=".ListarActivity"></activity>
    </application>

</manifest>